{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "python manage.py collectstatic --noinput"
  },
  "deploy": {
    "startCommand": "gunicorn bookmyseat.wsgi --log-file -",
    "restartPolicyType": "ON_FAILURE"
  }
}
